{
  "name": "nexus-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "chmod +x build.sh && ./build.sh",
    "build:simple": "cd frontend && npm install && npm run build"
  }
}