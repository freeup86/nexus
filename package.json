{
  "name": "nexus-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "chmod +x build.sh && ./build.sh",
    "build:frontend": "cd frontend && npm install && CI=true npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  }
}